## 2026-02-27 14:58 CST — Pachano EN/ES i18n SEO P0 (hreflang por ruta)

- Se cerró fix técnico principal de hreflang para `projects/hector-services-site` con commit:
  - `18eb352` — `fix(i18n-seo): route-specific hreflang and locale alternates`.
- Cambios aplicados:
  - `lib/seo/meta.ts`: nuevo `buildLocaleAlternates(path)` para alternates por ruta con lógica de paridad real ES/EN (incluye `x-default`).
  - `app/layout.tsx`: removidos alternates hardcodeados en `<head>` que forzaban `/` y `/en` en todas las páginas; ahora usa alternates por ruta.
  - `app/en/layout.tsx`: alternates por helper central.
  - `app/cases/[slug]/page.tsx`: alternates dinámicos por slug.
- Validación real ejecutada:
  - `npm run build` ✅.
  - `curl` en `/services` y `/en/services` muestra hreflang recíproco correcto (`es/en/x-default`) ✅.
  - `curl` en `/services/seo-aeo-b2b` (ES-only) no publica hreflang EN inexistente ✅.
  - `html lang` correcto (`/`=es, `/en/about`=en) y `og:locale` (`es_MX`/`en_US`) ✅.
  - Schema `inLanguage` (`/`=es, `/en`=en) ✅.
- Pendiente fuera de código: validación GSC (Coverage + URL Inspection + re-submit sitemap/URLs prioritarias).

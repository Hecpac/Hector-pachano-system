# 2026-02-23

- Recordatorio pendiente TCinsurance: cuando Hector tenga el valor, agregar `NEXT_PUBLIC_META_PIXEL_ID` (Meta Pixel) en `projects/hector-services-site/.env.local` y en variables de Vercel.
- Cron revisado: se corrigió `weekly-summarize-eval` agregando delivery Telegram a chat `574707975` (antes fallaba por "cron delivery target is missing"). Persisten dos jobs con último estado error por timeout/model cooldown: `healthcheck:security-audit` y `healthcheck:update-status`.
- Se parchó cron healthcheck para reducir fallos: `healthcheck:security-audit` ahora usa `openclaw security audit` + `openclaw status` (sin --deep), timeout 900s y modelo `google/gemini-3.1-pro-preview`; `healthcheck:update-status` fijado a modelo Gemini y timeout 300s.
- Se aplicó "siguiente nivel" del video a operación diaria:
  - Reverse prompting y auto-mejora integrados en playbooks/plantillas (`WORKFLOW_AUTO.md`, `playbooks/AUTO_IMPROVE_LOOP.md`, templates start/close, gate sensible).
  - Flujo diario Mission Control documentado en `playbooks/MISSION_CONTROL_DAILY_FLOW.md`.
- Cron actualizado para alinear flujo operativo:
  - `daily-morning-brief-hector` (id `19660203-beec-4510-af65-009323f239ad`) reforzado con reverse-prompt interno + salida morning->prioridades->approvals->closeout.
  - Nuevo cron `mission-control-approvals-check` (id `0130b33d-aeed-45e1-b817-3bd177afe4db`) a las 13:10 CT.
  - Nuevo cron `mission-control-closeout` (id `8eeb153d-6ddf-4be6-972f-ebaa4e665adc`) a las 18:05 CT.
- Mission Control UI integrado con estado real de crons OpenClaw en `/` (server-side, sin client fetching), con fallback de error visible y estilos responsive.
- Commit: `8dad599` (`feat(mission-control): show real openclaw cron status on dashboard`).
- Hector reforzó regla de calidad: prohibido inventar o responder sin validar fuentes; siempre revisar bien antes de responder.
- Optimización de stack LLM aplicada:
  - `~/.openclaw/openclaw.json`: se añadió `google/gemini-2.5-flash`, Sonnet/Opus permitidos, fallbacks optimizados (spark -> sonnet -> gemini-pro), subagentes con límites (depth 2, children 3, concurrent 6) y loopDetection activado (schema compatible).
  - Cron re-ruteado por herramienta: Flash para checks cortos; Codex/Codex Spark para operación técnica; Sonnet para research editorial; Gemini Pro para briefs/noticias globales.
- Playbook creado: `playbooks/LLM_STACK_OPTIMIZED.md`.
- Se configuró Codex multi-agent (CLI) en `~/.codex/config.toml` con `multi_agent=true`, `agents.max_threads=6` y roles `default`, `worker`, `explorer` (read-only + spark), `reviewer` (read-only + high reasoning) usando archivos en `~/.codex/agents/*.toml`.
- Ejecutados fixes SEO solicitados (P0/P1) en producción:
  - pachanodesign.com: títulos/meta de servicios reforzados para intención B2B y metadata base ajustada.
  - tcinsurancetx.com: slugs de blog corruptos normalizados + redirects permanentes (308) desde slugs legacy; title de /servicios actualizado.
- Verificación en vivo:
  - redirects legacy -> slug limpio OK (4 rutas).
  - titles/meta nuevos visibles en /services/seo-aeo, /services/diseno-web, /services/automatizaciones y /servicios.
- TC Insurance blog images fixed: added curated assets under `public/blog/*`, remapped active post `featuredImage` paths, and updated `/blog` list UI to render thumbnails per post card. Deployed to production and verified image URLs resolve from tcinsurancetx.com.
- TC Insurance blog covers redone per contenido: se crearon 8 portadas temáticas en `public/blog/posts/*` (generadas localmente según tema de cada artículo), se mapearon en `src/data/blogPosts.ts` y se desplegó a producción. Verificado: `/blog` ahora referencia las 8 imágenes nuevas.
- TC Insurance blog portada editorial v2: se reemplazaron TODAS las imágenes del blog por set nuevo consistente estilo periódico (`public/blog/editorial/*`), con mapeo por artículo actualizado en `src/data/blogPosts.ts` y deploy a producción validado.
- Auditoría móvil TC Insurance ejecutada (Lighthouse + verificación manual).
- Fixes aplicados en producción:
  - Carrusel servicios: dots y flechas ahora garantizan hit area >=44px (mobile a11y).
  - Hero: ajuste de carga de imagen de fondo para reducir competencia de recursos críticos.
- Validación posterior:
  - `/servicios` mobile: small tap targets = 0.
  - Lighthouse móvil home quedó estable en a11y/seo 100; performance con variación de corrida (~86-87).
- Ejecutada fase técnica SEO en TC Insurance:
  - Post `gastos-finales` migrado a slug SEO `seguro-gastos-finales-texas-guia-completa` con redirect 308 desde slug anterior.
  - Contenido expandido a ~1,300+ palabras (medición render ~1,637 en main), FAQ visible y `FAQPage` schema.
  - Hardening de indexación: metadata global con `robots index,follow` + directivas Googlebot.
  - Deploy productivo validado en `https://tcinsurancetx.com`.
